csvSum <- function(A, B){
  return (c(A, B))
}

csvTime <- function(df, fileName){
  write.csv(df, fileName)
}
csvFreq <- function(df, fileName){
  write.csv(df, fileName)
}
csvCorr <- function(df, fileName){
  write.csv(df, fileName)
}
csvLya <- function(df, fileName){
  write.csv(df, fileName)
}